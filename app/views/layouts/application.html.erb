<!DOCTYPE html>
<html>
<head>

	<title>AboveAverage</title>
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>
	<%= csrf_meta_tags %>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	


</head>

	

	<nav class="navbar navbar-default" role="navigation">

		<div class="container-fluid">
			<!-- Brand and toggle get grouped for better mobile display -->
			<div class="navbar-header">
				<!-- <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
					<span class="sr-only">Toggle navigation</span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
				</button> -->
				
				<ul class="nav navbar-nav">
					<li class="divider"><%= link_to "A / A", root_path %></li>
				</ul>
				
			</div>

			<!-- Collect the nav links, forms, and other content for toggling -->
			<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
				<ul class="nav navbar-nav">

					
					<li class="divider">
						<%= link_to "About", about_path %>
					</li>
					<% if !current_user.nil? %>
						<li class="divider">
							<%= link_to "User Home", user_path(:id => session.id), method: :get %>
						</li>
					<% end %>
					<% if current_user.nil? %>
						<li class="divider"><%= link_to "Sign Up", new_user_registration_path %></li>
					<% end %>
					<li class="divider">
						<%= link_to_if current_user.nil?, 'Sign-in', new_user_session_path, method: :get do link_to 'Sign-out', destroy_user_session_path, method: :delete end %>
					</li>
					<li class="divider">
						<%= link_to_if !current_user.nil?, "Results", mastergraph_path, method: :get do link_to "Preview Results", example_path, method: :get end %></li>



					<li class="divider">
						<%= link_to 'nycda', "http://nycda.com", target: "_blank" %>
					</li>

					<li class="divider">
						<%= link_to 'devise', "https://github.com/RailsApps/rails-devise", target: "_blank" %>
					</li>

				</ul>
			</div>
		</nav>

		<% if flash[:notice] %> 
			<p class="notice"><%= notice %></p>
		<% end %>
		<% if flash[:alert] %>
			<p class ="alert"><%= alert %></p>
		<% end %>

	
		<body>
	<div class="body">
			
		<%= yield %>
			</div>
	</body>
</html>
